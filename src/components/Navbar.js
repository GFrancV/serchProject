import React from 'react';

function Navbar() {
    return ( 
        <nav className = "navbar navbar-dark bg-dark" >
            <a className="text-default" href="#">Navbar</a>
        </nav>
    );
}

export default Navbar;